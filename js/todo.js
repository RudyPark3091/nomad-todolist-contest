// not needed in javascript (not typescript)

class Todo {
  constructor({ id, title, content, due, done }) {
    this.id = id;
    this.title = title;
    this.content = content;
    this.due = due;
    this.done = done;
  }
}

export default Todo;
